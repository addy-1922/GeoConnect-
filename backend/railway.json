{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "healthcheckPath": "/api/health/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT config.asgi:application"
  },
  "hooks": {
    "prebuild": "cd ../frontend && npm ci && npm run build && rm -rf ../backend/frontend_dist && mkdir -p ../backend/frontend_dist && cp -r dist/* ../backend/frontend_dist/"
  }
}